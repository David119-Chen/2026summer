# 範本：複製成 .env 並填入實際密碼。.env 已被 .gitignore 擋下，不會上傳。
#   PowerShell:  Copy-Item .env.example .env
#   bash:        cp .env.example .env
# 然後把下面換成真正的密碼，之後直接 `node build.mjs` 即可。
HANDBOOK_PW=your-handbook-password
