# === 安全規則：避免未加密內容外洩 ===
# 預設忽略所有 HTML 檔，一律保留本地：
#   - 北海道隨身手冊-網頁版.dc.html（內容母片，未加密、含行程明文，build.mjs 的來源）
#   - 北海道隨身手冊-離線版.html（離線版，未加密、含行程明文，手動傳手機）
#   - shell.html（解鎖畫面 + 解密腳本外殼，建置資產）
#   - 北海道隨身手冊-加密封存-密碼鎖.html（原始加密封存，~18MB，建置已不讀，留作備份）
*.html

# build.mjs 用的本地建置資產（不含行程明文，但屬建置輸入，保留本地）
support.js

# 發布到 GitHub Pages 的檔（皆不含行程明文）：
#   - index.html：線上版（已加密、字型走 CDN、約 140KB），由 build.mjs 從 .dc.html 產生
#   - 北海道隨身手冊-離線版-密碼鎖.html：舊網址相容用的純轉址頁，導向 index.html
# 注意：這個轉址頁名稱剛好含「密碼鎖」，但它只是 redirect，切勿在此放真內容。
!index.html
!北海道隨身手冊-離線版-密碼鎖.html

# 建置時抓下來的 React/ReactDOM/Babel 快取，不需進版控
.vendor-cache/

# 預覽用的本地腳手架，不需進版控
.claude/launch.json

# 常見暫存／系統檔
.DS_Store
Thumbs.db
*.tmp
*.bak
