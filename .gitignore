# === 安全規則：避免未加密原稿外洩 ===
# 預設忽略所有 HTML 檔（未加密原稿、離線版母片一律保留本地）
#   - 北海道隨身手冊-離線版-密碼鎖.html（離線母片，~18MB，手動傳手機）
#   - *.dc.html（未加密原稿）
*.html

# 唯一發布到 GitHub Pages 的檔：線上版（已加密、字型走 CDN、約 140KB）
# 由 build-online.mjs 從離線母片產生；下載到的只是密文，無密碼讀不到行程。
!index.html

# 預覽用的本地腳手架，不需進版控
.claude/launch.json

# 常見暫存／系統檔
.DS_Store
Thumbs.db
*.tmp
*.bak
