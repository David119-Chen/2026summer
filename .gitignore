# === 安全規則：避免未加密內容外洩 ===
# 預設忽略所有 HTML 檔，一律保留本地：
#   - 北海道隨身手冊-離線版-密碼鎖.html（加密母片／建置來源，~18MB）
#   - 北海道隨身手冊-離線版.html（離線版，未加密、含行程明文，手動傳手機）
#   - *.dc.html（未加密原稿）
*.html

# 唯一發布到 GitHub Pages 的檔：線上版（已加密、字型走 CDN、約 140KB）
# 由 build.mjs 從加密母片產生；下載到的只是密文，無密碼讀不到行程。
!index.html

# 建置時抓下來的 React/ReactDOM/Babel 快取，不需進版控
.vendor-cache/

# 預覽用的本地腳手架，不需進版控
.claude/launch.json

# 常見暫存／系統檔
.DS_Store
Thumbs.db
*.tmp
*.bak
